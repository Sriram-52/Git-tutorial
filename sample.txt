Git commands:

for making pull req:
git status(pop up the files that are changed)
git add **files**
git commit -m "Commit message"
git checkout -b "branch-name"
git push(pop's an error paste the command)
git checkout "master branch"

for cloning a repo:
git clone "repo url"